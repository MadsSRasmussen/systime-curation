export type PixelPosition = {
    x: number,
    y: number,
}

export type PercentagePosition = {
    x: number,
    y: number,
}

export type PercentageDimensions = {
    width: number,
    height: number,
}

export type PixelDimensions = {
    width: number,
    height: number,
}

export type NormalizedDimensions = {
    width: number,
    height: number,
}